# CoursePlatform Keycloak Theme Configuration

# Parent theme - inherit from keycloak base theme
parent=keycloak

# Stylesheets to include
styles=css/login.css

# Import base theme styles
import=common/keycloak

# Locales supported
locales=en

# Default locale
defaultLocale=en
