using Courses.Application.Abstractions;

namespace Courses.Infrastructure.UrlResolver;

internal class UrlResolver : IUrlResolver
{
    public string Resolve(string relativePath)
    {
        return string.Empty;
    }
}
